# 검증 규칙

- 코드 변경 후 완료·배포 전에 `python -X utf8 run_tests.py`를 실행한다.
- `C:\Users\Public\scan`의 모든 PDF를 개별로 검사하고, 호환되는 양식끼리 2개부터 전체 개수까지 모든 묶음 크기를 검사한다. 리더·마음·여행의 실제 분석 및 출력 위치 회귀 검사도 포함한다.
- 빠른 부분 테스트만으로 전체 검증을 대신하지 않는다. 자료 누락, 접근 오류, 생략된 검사는 통과로 보고하지 않는다.
- 실패가 있으면 원인과 파일·페이지를 확인하고 수정한다. 양식 불일치를 숨기거나 검사 기준을 임의로 낮춰 통과시키지 않는다.
- 개인 스캔 PDF, 프리셋, 분석 결과는 Git에 추가하지 않는다.
